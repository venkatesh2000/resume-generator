import NavButtons from "../NavButtons/NavButtons.jsx";

const Preview = (props) => {
  return (
    <div>
      <NavButtons {...props} validateInfo={() => {}} />
    </div>
  );
};

export default Preview;
